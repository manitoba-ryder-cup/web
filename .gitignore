node_modules
dist
*.local
.DS_Store
# local dev keypair (heimdall/scorecard)
dev/keys
# design/spec docs are kept local, not committed
docs/superpowers

# Written by dev/bootstrap.sh — the tenant heimdall minted for this environment.
.env
