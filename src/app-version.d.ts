// Substituted at build time by vite.config's define, so it is a literal in the bundle rather
// than a lookup: nothing reads it at runtime and there is no env var to be missing.
declare const __APP_VERSION__: string
